apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mssql-payment-pvc
spec:
  storageClassName: manual   #somente informar quando houver pv criado
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Mi